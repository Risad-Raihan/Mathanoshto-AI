# Core LLM APIs
tavily-python>=0.3.0
google-generativeai>=0.3.0
openai>=1.0.0
anthropic>=0.21.0

# Configuration Management
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0.0

# Authentication & Security
bcrypt>=4.0.0
cryptography>=41.0.0

# Database
SQLAlchemy>=2.0.0

# Memory System & Embeddings
chromadb>=0.4.0  # Vector database
sentence-transformers>=2.2.0  # Local embeddings
torch>=2.0.0  # Required for sentence-transformers
numpy>=1.24.0  # Already included but ensure version

# File Handling
Pillow>=10.0.0  # Image processing and manipulation
PyPDF2>=3.0.0
pdfplumber>=0.10.0  # Better PDF text extraction
PyMuPDF>=1.23.0  # PDF thumbnail generation (fitz)
python-docx>=1.0.0  # DOCX text extraction
python-magic>=0.4.27
pytesseract>=0.3.10  # OCR for images
pandas>=2.0.0  # CSV parsing
openpyxl>=3.1.0  # Excel support
python-multipart>=0.0.6  # File upload support

# Image Generation (Optional - for enhanced features)
# Note: DALL-E uses OpenAI API (already included)
# stability-sdk>=0.8.0  # Stability AI (optional, uncomment if needed)

# Token Counting
tiktoken>=0.5.0

# Async Support
aiohttp>=3.9.0
asyncio>=3.4.3

# Web Scraping (Phase 4)
beautifulsoup4>=4.12.0
requests>=2.31.0
playwright>=1.40.0  # JavaScript rendering
readability-lxml>=0.8.1  # Smart content extraction
html2text>=2020.1.16  # HTML to markdown
# reppy>=0.4.14  # robots.txt parser (optional, has C++ compilation issues on some systems)
urllib3>=2.0.0  # URL handling
lxml>=4.9.0  # Fast HTML parsing
firecrawl-py>=0.0.1  # Firecrawl API (optional)

# YouTube Tools (Phase 5)
youtube-transcript-api>=0.6.0  # Get video transcripts
pytube>=15.0.0  # Video metadata
isodate>=0.6.1  # Parse ISO 8601 durations

# Data Analysis Tools (Phase 6)
matplotlib>=3.7.0  # Plotting
seaborn>=0.12.0  # Statistical visualizations
plotly>=5.17.0  # Interactive charts
scipy>=1.11.0  # Scientific computing
sqlalchemy>=2.0.0  # SQL interface (already included above)
tabulate>=0.9.0  # Pretty table printing

# Frontend (Streamlit)
streamlit>=1.30.0
streamlit-chat>=0.1.1

# Diagram Generation (using Kroki service - no local packages needed)
# All diagrams are rendered via https://kroki.io API
# Only optional dependencies for advanced SVG processing:
# cairosvg>=2.7.0  # Optional: SVG to PNG conversion
# svglib>=1.5.0  # Optional: SVG processing

# Testing (Development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Code Quality (Development)
black>=23.0.0
flake8>=6.1.0
mypy>=1.7.0

